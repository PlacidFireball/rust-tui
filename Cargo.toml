[package]
name = "rust-tui"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "*"
serde = { version = "*", features = ["derive"] }
serde_json = "*"
clap = { version = "*", features = ["derive"] }
